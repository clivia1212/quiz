// import axios from 'axios';
import Vue from 'vue';
import App from './App.vue';

new Vue({
  el: '#app',
  // data: {a: 1},
  render: (createElements, context) => createElements(App)
  // error
  // template: 'dsd',
  // components: { App }
});

